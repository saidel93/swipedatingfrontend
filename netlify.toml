[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

# Netlify's official Next.js runtime (handles pages, API routes, images)
[[plugins]]
  package = "@netlify/plugin-nextjs"
